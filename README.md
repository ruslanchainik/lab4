# Лабораторная 4

1) Необходимо будет построить односвязную сеть для бинарной классификации в основе которой будет BReLU + Sigmoid. Реализовать самостоятельно, без использования фреймворков.

2) Необходимо реализовать SGD с регулируемым размером батча и модификацией Adam(связка Momemntum/Nesterov + AdaGrad/RMSProp)

3) Необходимо сгенерировать два датасета по заданному шаблону:

    1) ```python
         X, y = make_moons(n_samples=400, noise=0.15, random_state=НОМЕР_ИСУ)
        ```

    2) ```python
         X, y = make_classification(n_samples=200, n_features=5, n_redundant=2, random_state=НОМЕР_ИСУ,
                           n_informative=2, n_clusters_per_class = 2, n_classes=2)   
        ```

4) Необходимо обучить сеть используя в качестве функции потерь `Cross-entropy`. Данные необходимо разделить на тренировочные (60%), валидационные (20%) и тестовые (20%). Кросс-валидацию реализовывать НЕ нужно. (Модель необходимо хорошо обучить, т.е. подобрать оптимальные гиперпараметры и разобраться как выбрать наилучшую модель).

5) Реализовать перцептрон с одним скрытым слоем и повторить пункт 4.

6) Сформировать отчет, в котором будет отражено то как обучались модели и на сколько они хорошо предсказывают классы.
